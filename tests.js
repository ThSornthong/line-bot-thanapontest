/*var cron = require('node-cron');

cron.schedule('5 * * * * *', () => {
    console.log('running a task every minute');
  });*/
for (let index = 1; index < 4; index++) {
  for (let indexs = 0; indexs < 6; indexs++) {
    console.log('*')
    
  }
  console.log('$n')
}